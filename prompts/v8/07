Review and validate all enemy AI production, strength-evaluation, attack, retreat, and regroup changes
from this prompt series as one integrated system.

Check for conflicting rules, stale state, hidden-information cheating, decision oscillation, invalid
targets, and groups becoming stuck between states. Confirm that the implemented decision precedence is:

1. Hard-safety conditions may force retreat or terminate an attack.
2. A fresh and sufficiently strong advantage may override casualty-based retreat.
3. A weaker assessment causes retreat.
4. An uncertain or stale assessment uses the documented conservative fallback.

Add or extend a deterministic integration scenario in which:

- The player reveals many archers
- The enemy AI shifts production toward shields and away from swordsmen
- An enemy AI group initially judges itself weaker and retreats
- Newly observed reinforcements make the group stronger and it re-engages
- The group takes heavy casualties but continues attacking while it remains stronger
- Further losses make the group weaker and trigger retreat

Also verify ordinary production without an archer threat, lost vision, stale intelligence, combat-group
cleanup, base defense, unreachable targets, victory, defeat, pause, restart, and deterministic seeded
behavior. Fix regressions caused by this prompt series, but do not perform unrelated refactoring or
balance changes.

After implementation:

1. Run all focused AI tests and the complete automated test suite.
2. Run `python3 -m py_compile main.py simulate_enemy_ai.py tests/test_main.py`, adjusting the file list
   only if the project structure has legitimately changed.
3. Run fixed-seed headless scenarios covering production, retreat, regroup, re-engagement, casualty
   overrides, and final retreat.
4. Launch the game for a short end-to-end smoke test if the environment supports it.
5. Save screenshots only under `/work/screenshots`. After every screenshot, inspect it immediately with
   `view_image` and report all clipping, overlap, or other visual problems.
6. Clearly separate verified behavior from subjective balance observations.
7. Report changed files, configuration values, exact test results, and remaining gameplay-tuning
   assumptions.

Do not commit changes.
