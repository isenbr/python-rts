Work in /work on the python-rts project. Continue from the separated roster/entity model. Do not commit
changes.

Replace both bases with kings and create the initial knight guards.

Requirements:
- On every reset, spawn one green king at PLAYER_BASE_POSITION and one red king at
  ENEMY_BASE_POSITION. Do not also create a Base at either location.
- Spawn exactly two same-team knights beside each king: one at a fixed guard post above the king and
  one at the mirrored guard post below it. Use named offsets and keep positions inside the map.
- Give every knight a persistent home/guard-post position for later leash behavior.
- Kings are stationary objectives. They must not become selected by select-all, kind selection,
  drag-selection, clicking, or any other player selection path.
- Knights also must not become selected or receive player orders.
- Exclude kings and knights from EnemyAI squads, reserves, defenders, formations, production
  investment, wave composition, retreat logic, and tactical destinations.
- Change recruitment spawning to use the appropriate team king as its anchor. Spawn new green units in
  front of the green king toward the battlefield, and new red units in front of the red king toward
  the battlefield. Keep deterministic spacing and prevent the initial guards from affecting the
  recruit-slot sequence.
- Update terrain quiet zones, starting camera assumptions, and initial objective message only where
  required by the replacement.
- Preserve fog-of-war fairness: the red king and red knights must not render or become directly
  targetable by the player while hidden. Friendly vision should originate from the friendly king with
  the same intended starting visibility formerly supplied by the base.

Add deterministic tests covering reset entity counts and positions, mirrored guard placement, stored
home positions, non-selection/non-ordering, exclusion from EnemyAI-managed collections, front-of-king
recruit spawning for both teams, deterministic spawn slots, and hidden enemy objective behavior.

Checks:
1. Run the focused spawning/control tests twice and verify identical results.
2. Run `python3 -m py_compile main.py simulate_matchup.py simulate_enemy_ai.py`.
3. Run `python3 -m unittest discover -s tests`.
4. Inspect `git diff --check` and search for any remaining construction of Base during normal reset.
5. If a screenshot is taken, save it only under `/work/screenshots`, immediately inspect it with
   `view_image` using its absolute path, and report every visual problem.

Do not implement autonomous guard chasing or final visuals yet. Do not stop with failing checks.
