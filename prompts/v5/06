Prevent every archer, green or red, from firing while moving, then perform a complete integration check
for the shield unit and smaller map.

Define "moving" from actual displacement or an explicit per-update movement state, not merely from the
presence of a stale target position. During an update in which an archer moves a meaningful distance, it
must not attack. It may fire once stationary and in range, subject to its existing cooldown. Preserve
normal attack-move behavior: archers should stop and shoot when they acquire an in-range target, then
resume their order when appropriate. Enemy kiting must alternate movement and firing rather than
producing shots during retreat displacement.

Keep swordsmen and shields able to use their existing melee behavior. Do not change damage, range,
cooldown, speed, cost, health, income, fog rules, or map size while implementing this rule.

Add deterministic tests covering:

- A green archer cannot damage a target or create an arrow on an update where it moves
- A red archer cannot damage a target or create an arrow while tactically retreating/repositioning
- An archer fires after it has stopped and its cooldown permits
- A stationary archer with an in-range target still fires immediately
- A stale or already-reached move destination does not incorrectly block firing
- Archers do not exceed their normal cooldown rate through alternating movement updates
- Swordsman and shield melee attacks remain unchanged
- Player attack-move, enemy target selection, kiting, rallying, waves, and defense still function

Then perform an integration regression pass covering:

- Shield cost 300, health 200, speed 0.7, cooldown 1.0, damage 5, and range 1.02
- Shield rendering approximately 15% larger and visually distinct
- Shield recruitment, HUD count, shortcuts, and selection
- Enemy shield production and frontline behavior
- The 120 x 120 map and all world-bound clamping
- Menu, pause, restart, fog, camera, combat, victory, and defeat

After implementation:

1. Run the new archer tests.
2. Run the complete automated test suite.
3. Run `python3 -m py_compile main.py simulate_enemy_ai.py tests/test_main.py`.
4. Run fixed-seed headless scenarios for stationary firing, attack-move, enemy kiting, mixed-unit waves,
   and base defense.
5. Launch the game for a full smoke test using all three unit types.
6. Take screenshots only under `/work/screenshots`. Immediately inspect every screenshot with
   `view_image` and report all visual problems; fix verified regressions and repeat the relevant checks.
7. Clearly separate verified behavior from subjective balance observations, list files changed, and
   identify any remaining limitations.

Do not commit changes.
