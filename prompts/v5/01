Inspect the current Verdant Crown project and prepare it for a third unit type and a smaller map
without changing gameplay yet.

Before editing, run the existing automated tests and a syntax/compilation check to establish a baseline.
Identify and document every place that assumes:

- The only unit kinds are "swordsman" and "archer"
- The map is exactly 200 x 200
- World bounds end at 199.5
- Bases, starting units, roads, rally points, camera positions, or AI behavior use fixed coordinates
- Unit rendering uses one shared size

Refactor repeated unit-kind collections, map bounds, and starting-position calculations into named
constants or small helpers where this can be done without changing behavior. Preserve the current
200 x 200 map, unit statistics, costs, controls, visuals, AI decisions, and starting layout in this
step. Do not add the shield unit yet.

Add focused regression tests that lock down the current behavior most likely to be affected later,
including:

- All existing unit statistics and costs
- Initial bases and units being inside the map
- Recruited units spawning inside the map
- Move orders and movement clamping to the map bounds
- Enemy rally and formation destinations being inside the map
- Existing swordsman/archer selection and recruitment

After implementation:

1. Run the full automated test suite and report the exact result.
2. Run `python3 -m py_compile main.py simulate_enemy_ai.py tests/test_main.py`.
3. Run a short headless smoke test that creates, updates, and draws a game.
4. If any screenshot is taken, save it only under `/work/screenshots`, inspect it immediately with
   `view_image`, and report any clipping, overlap, or other visual problem.
5. Report files changed, assumptions removed, and any remaining hard-coded map or unit-kind logic.

Stop after this preparatory refactor. Do not commit changes.
