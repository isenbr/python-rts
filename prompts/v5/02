Add a new shield unit to Verdant Crown's core model and combat simulation, building on the preceding
preparatory refactor.

Use these explicit initial balance values:

- Recruitment cost: 300 essence
- Maximum and starting health: 200, exactly double the swordsman's 100
- Movement speed: 0.7, slower than the swordsman's 1.0
- Attack cooldown: 1.0 seconds, slower than the swordsman's 0.5 seconds
- Damage: 5
- Attack range: 1.02

Represent these values through the same centralized unit-stat mechanism used by the existing units.
Avoid a catch-all `else` branch that silently treats unknown kinds as archers. Invalid unit kinds
should fail clearly and consistently without spending essence.

Make shield units work with generic movement, targeting, melee attacks, damage, death cleanup, fog of
war, selection state, and victory/defeat behavior. A shield attack should use the melee effect, not an
arrow. Do not add HUD controls, special artwork, enemy production, or change the map size in this step.

Add deterministic tests covering:

- The shield's cost and every requested statistic
- Health being exactly twice swordsman health
- Speed and attack rate being lower than the swordsman's
- Successful recruitment at exactly 300 essence
- Rejected recruitment below 300 essence without changing the wallet or army
- Invalid unit kinds failing without changing game state
- Shield movement distance over a fixed time step
- Shield cooldown, melee damage, target acquisition, death, and target cleanup
- Both green and red shield units remaining compatible with fog and combat

After implementation:

1. Run the new focused tests.
2. Run the complete automated test suite.
3. Run `python3 -m py_compile main.py simulate_enemy_ai.py tests/test_main.py`.
4. Run a deterministic headless combat smoke test with shields against swordsmen and archers.
5. Report the exact test results and confirm that existing swordsman and archer values did not change.

Do not implement the shield UI, visuals, AI production, or map resize yet. Do not commit changes.
