Continue from the previous stage. Work in /work and do not commit changes.

Teach navigation to account for traversable terrain speed without changing the
required speed multipliers.

Requirements:

1. A* must use travel time, not raw geometric distance, as terrain traversal
   cost. Faster path tiles should be attractive when their time savings justify
   a detour; slower forest and mountain tiles should be avoided when a quicker
   route exists.
2. Preserve the existing dynamic hard obstacles, soft moving-allies costs,
   deterministic side preference, no-corner-cutting rule, expansion limit,
   waypoint smoothing, and path-cache behavior.
3. Keep all terrain kinds traversable. Do not add impassable mountains.
4. Make the A* heuristic admissible with a maximum terrain multiplier of 2.0.
   Document the cost units and heuristic choice.
5. Waypoint smoothing must not erase a beneficial terrain route by replacing it
   with a geometrically clear but slower straight segment. Either make smoothing
   terrain-cost-aware or retain the necessary waypoints.
6. Direct movement may remain valid when unobstructed, but it must not blindly
   bypass a substantially faster path route. Use a bounded, measurable policy
   that avoids running A* every frame and does not cause path thrashing.
7. Terrain changes on reset must invalidate any cached paths and cost data.
8. Preserve deterministic results and the A* expansion safety limit.

Add tests proving:

- a unit takes a modest path-tile detour when it arrives sooner;
- a unit does not take an excessively long path detour that arrives later;
- a unit avoids a mountain or forest band when a faster route exists;
- a unit crosses slow terrain when it is still the quickest route;
- smoothed waypoints retain a beneficial path route;
- diagonal cost and terrain cost compose correctly;
- identical maps and orders produce identical waypoints;
- cached paths do not replan continuously on static terrain;
- reset cannot reuse a route from the previous terrain map;
- existing dynamic unit-obstacle and no-corner-cutting behavior remains intact;
- failed-route expansion remains within UNIT_ASTAR_MAX_EXPANSIONS;
- large maps remain within the established navigation performance envelope.

Checks:

1. Run focused pathfinding and terrain-routing tests.
2. Run movement baseline, stress, collision, shared-separation, and navigation
   performance tests.
3. Run the complete test suite.
4. Inspect path calculation counts and maximum expanded nodes in representative
   simulations; report before/after values where available.
5. Run `python3 -m py_compile main.py simulate_performance.py`.
6. Run `git diff --check` and `git status --short`.
7. If a screenshot is taken, follow the repository screenshot rules.

Do not implement the final terrain art in this step. Do not stop with failing
checks or hide performance regressions by loosening assertions without cause.
