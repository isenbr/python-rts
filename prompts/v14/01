Work in /work on the python-rts project. Do not commit changes.

Inspect and characterize the current terrain, rendering, movement, pathfinding,
level-reset, spawning, camera, fog, and test code before implementing gameplay
terrain.

The eventual terrain system will contain four tile types:

- mountain: movement speed multiplier 0.5;
- forest: movement speed multiplier 0.75;
- path: movement speed multiplier 2.0;
- plains: movement speed multiplier 1.0.

Every tile type will have a distinct base color, a simple code-drawn 2D graphic,
and exactly four deterministic visual variations. Visual variation must never
change gameplay behavior.

Map and document:

1. How make_terrain(), terrain_tones, terrain_details, and draw_terrain() work.
2. Every place movement distance or unit.speed is used.
3. How direct movement, local separation, cached waypoints, A*, and corridor
   checks interact.
4. How world positions map to terrain cells, including map edges.
5. How levels, kings, guards, starting troops, recruits, and move destinations
   are initialized.
6. Which tests assume that terrain contains only road cells or is empty.
7. Whether simulations or performance tests construct Game without rendering.

Do not change gameplay in this step. Add focused characterization tests only
where later terrain work could accidentally alter unrelated movement behavior.

Checks:

1. Run `python3 -m py_compile main.py simulate_matchup.py simulate_enemy_ai.py simulate_performance.py`.
2. Run terrain-, level-, movement-, and pathfinding-related tests.
3. Run `python3 -m unittest discover -s tests`.
4. Run `git diff --check` and `git status --short`.
5. Report the exact movement call sites that must use terrain speed.
6. Report existing tests that later prompts must update.
7. If a screenshot is taken, save it only under `/work/screenshots`, immediately
   inspect it with `view_image` using its absolute path, and report every visual
   problem.

Finish with the smallest safe implementation order. Do not implement terrain
gameplay or graphics yet, and do not stop with failing checks.
