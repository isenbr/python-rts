Save screenshots only under /work/screenshots.

After taking each screenshot call view_underscore image with its absolute path, inspect the rendered UI, and report any visual problems.

Never commit changes.